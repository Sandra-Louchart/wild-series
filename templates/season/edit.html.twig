{% extends 'base.html.twig' %}

{% block title %}{{ 'edit.season'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'season'|trans }}</h1>

    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('season_index') }}">{{ 'back.season'|trans }}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
